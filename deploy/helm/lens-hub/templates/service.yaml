apiVersion: v1
kind: Service
metadata: {name: {{ .Release.Name }}-hub}
spec:
  type: {{ .Values.service.type }}
  selector: {app.kubernetes.io/name: lens-hub, app.kubernetes.io/instance: {{ .Release.Name }}}
  ports: [{name: http, port: {{ .Values.service.port }}, targetPort: http}]
