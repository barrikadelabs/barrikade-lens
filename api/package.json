{
  "name": "@barrikade/lens-api",
  "version": "0.1.0",
  "description": "Telemetry ingest service for Barrikade Lens — receives anonymous scan metrics from the CLI.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.9.0",
    "express": "^5.1.0"
  }
}
